# CorrectClick — Developer Notes

## Project overview
macOS menu-bar app + Finder Sync Extension that adds a "CorrectClick" submenu to the Finder right-click menu for quick file creation. See the project spec at the top of any conversation for full feature details.

## Structure
- **CorrectClick/** — parent app (menu-bar only, `LSUIElement = YES`)
- **CorrectClickExtension/** — Finder Sync Extension (`FIFinderSync` subclass)
- **project.yml** — xcodegen spec; run `xcodegen generate` after editing it to regenerate the `.xcodeproj`

## Build
```
xcodegen generate   # only needed after editing project.yml
xcodebuild -scheme CorrectClick ...
```
Open `CorrectClick.xcodeproj` in Xcode. Both targets use Automatic signing — the developer team must be set in Xcode's Signing & Capabilities (or via `DEVELOPMENT_TEAM` in project.yml).

## SourceKit false positives
SourceKit reports errors for `FIFinderSync`, `FIFinderSyncController`, `FIMenuKind` etc. because it doesn't index the FinderSync framework outside of Xcode. These are not real errors — the project builds and the symbols resolve correctly.

## Current status
**Feature complete**, plus Epic 1 (more file types), Epic 7 (menu-item preferences), Epic 2 (custom snippets/templates), and Epic 3 (Sparkle auto-update) from the feature backlog agents were given — Phases 1 through 3 of its suggested phasing. The extension is working:
- Right-clicking any folder in Finder shows a "CorrectClick" submenu
- File-creation actions: New Text File, New JSON File, New Python File, New CSV File, New Markdown File, New Shell Script (created executable), New YAML File, New HTML File (minimal boilerplate), New TOML File, New XML File, New .gitignore File, New LICENSE File, New .env File, New Dockerfile, New Swift File, New SQL File, New .plist File — plus New Text File from Clipboard and New PNG from Clipboard
- Files are created with auto-incrementing names (Untitled, Untitled 2, etc.; extensionless/dotfile types like `.gitignore` and `LICENSE` increment the same way — `LICENSE`, `LICENSE 2`, …)
- After creation, Finder selects the file and enters rename mode
- **Menu Items preferences:** the menu bar icon's "Preferences…" window (General / Menu Items / Snippets tabs) lets users hide/show and reorder any built-in menu action (two sections: New File, From Clipboard). Deleting isn't possible, only hiding, per the requirements doc. The `.gitignore`/`LICENSE` language/license pickers called out as a stretch goal (Epic 1a) were **not** implemented — both ship as empty files for now.
- **Snippets (custom templates):** the Preferences window's Snippets tab lets users define their own "New [X] File" menu entries — name, extension, starter content — with add/edit/delete/reorder/enable-disable and a live preview using dummy values. Starter content supports `{{date}}`, `{{time}}`, `{{datetime}}`, `{{filename}}`, `{{clipboard}}`, `{{author}}` placeholders (unknown placeholders are left as literal text, never stripped or crashed on). `{{author}}` is set once on the General tab. Enabled snippets appear as a third menu section, below "From Clipboard", ordered by their sort order. Import/export round-trips a JSON file of all templates via Export…/Import… buttons (merge-by-id on import, so re-importing a previously exported file is idempotent).
  - **Known limitation, not a bug:** `{{filename}}` resolves to the auto-generated stem the file is created with (e.g. "Untitled 2"), not whatever the user renames it to — Finder Sync extensions have no API to observe when an interactive Finder rename completes, so the true final name can't be known at content-write time. Documented per the requirements doc's explicit allowance to document this rather than solve it.
- **Auto-update (Sparkle):** the menu bar icon has "Check for Updates…", and the Preferences window's Updates tab has an automatic-checks toggle (daily) + a manual "Check for Updates Now" button. Full details — feed URL, signing key handling/rotation, the sandboxing setup — are in `RELEASING.md`, not repeated here. Short version: **the repo had to be made public** for the feed and DMG links to be fetchable without auth. Notarization (needed for the install step to reliably pass Gatekeeper) is done — see "To do" item 4 below.

## Known limitations
- **No menu in iCloud Drive / OneDrive / Dropbox folders.** Finder Sync extensions are exclusive per directory — the first extension registered for a folder (iCloud's or the cloud provider's own `FIFinderSync`/File Provider integration) is the only one Finder will invoke there. There is no API-level workaround; `directoryURLs` cannot override another provider's claim on a directory. (Confirmed via Apple Developer Forums threads on this exact issue, Aug 2026.) Not planned to be fixed — documented here so it isn't rediscovered as a bug.

## To do (shipping checklist)
1. **Icons** — done. `AppIcon.appiconset` has all sizes; generated via `scripts/generate_icon.swift` / `scripts/build_icns.sh`.
2. **DMG installer** — done. `scripts/build_dmg.sh` archives, exports with a real Developer ID signature (`DEVELOPMENT_TEAM: T4Q97VH6ST` in `project.yml`; ad-hoc fallback only kicks in if that signing ever becomes unavailable — see `developer_documentation/06-distribution.md`), notarizes and staples both the `.app` and the `.dmg`, and packages with `create-dmg` (custom background at `scripts/dmg_background.png`). Automated on every push to `main` via `.github/workflows/release.yml`: imports signing certs into a CI keychain, runs the test suite, bumps the patch version in the `VERSION` file, builds/notarizes, generates a signed appcast entry (see item 5), tags `vX.Y.Z`, and publishes a GitHub Release with the notarized DMG attached. **The repo is now public** (was private) — required so the DMG download link and the appcast feed are fetchable without authentication; see `RELEASING.md`.
3. **First-run onboarding** — done, tested working, needs a commit. `CorrectClick/OnboardingView.swift` + `CorrectClick/OnboardingWindowController.swift` replace the bare System Settings launch with a welcome window; `AppDelegate.swift` and `StatusBarController.swift` now route through `OnboardingWindowController.shared`. While testing this, found and fixed a pre-existing bug: plain `@main` on the `NSApplicationDelegate` class never wires `NSApp.delegate` (that auto-wiring was specific to the deprecated `@NSApplicationMain`), so `applicationDidFinishLaunching` was never called — meaning the status-bar icon has never worked, only the Finder submenu (which lives in the separate extension process and has its own lifecycle). Fixed by adding `CorrectClick/main.swift` that explicitly does `NSApplication.shared.delegate = AppDelegate(); NSApplication.shared.run()`, and removing `@main` from `AppDelegate.swift`.
4. **Notarise** — done. Apple Developer Program membership renewed under Team ID `T4Q97VH6ST`; both the Apple Development and Developer ID Application certificates exist; `scripts/ExportOptions.plist`'s `method` is `developer-id`. Verified for real, by hand, before wiring into CI: a Developer ID signed build was notarized (`Accepted`), stapled, and confirmed with `spctl -a -vvv` reporting `source=Notarized Developer ID` — both on the raw `.app` and on a copy extracted from a freshly-built `.dmg`. CI now does this automatically on every push (signing certs imported from GitHub secrets into a temporary keychain — see `RELEASING.md` for exactly which secrets and why two separate certificate types are needed).
5. **Sparkle auto-update** — done. SPM dependency, `UpdaterManager`, "Check for Updates…" menu item, Updates preferences tab, EdDSA keypair (public key in `Info.plist`; private key is a `SPARKLE_PRIVATE_KEY` GitHub Actions secret), and `scripts/generate_appcast.sh` wired into the release workflow. Update *checking* and the notarization it depends on for a real install are both now verified (see item 4) — what's still unverified is a live Sparkle check-download-install cycle end to end from a second machine against a real published release. See `RELEASING.md` for the full picture.

See `developer_documentation/` for deeper background: architecture, the Finder Sync extension, sandboxing/entitlements, file creation, build/debug, and distribution (one doc per topic, `01`–`06`). See `RELEASING.md` for the release/auto-update pipeline specifically.

## Key files
| File | Purpose |
|------|---------|
| `CorrectClickExtension/FinderSync.swift` | FIFinderSync subclass; builds the "CorrectClick" menu dynamically from `MenuPreferencesStore` (enabled items, in saved order) and dispatches to the right `FileCreator` action |
| `CorrectClickExtension/FileCreator.swift` | File creation logic, unique name generation (handles both `name.ext` and extensionless/dotfile names), rename-mode trigger |
| `CorrectClickExtension/CorrectClickExtension.entitlements` | Sandbox + filesystem temporary exception |
| `Shared/MenuItemPreferences.swift` | Compiled into **all three** targets (app, extension, tests — not a framework). Registry of every built-in menu item (`BuiltInMenuItems.all` — id, category, title) and `MenuPreferencesStore`, which reads/writes the shared `preferences.json` that both processes agree on. Also defines `RealHomeDirectory`, used by every Shared store below. See "Entitlements notes" below for why this needs its own real-path resolution. |
| `Shared/UserTemplate.swift` / `UserTemplateStore.swift` / `UserTemplateOperations.swift` | Epic 2 data model (`UserTemplate`: id, displayName, fileExtension, starterContent, sortOrder, enabled, createdAt), its shared `templates.json` store, and pure CRUD/merge helpers used by both the Snippets UI and import/export. |
| `Shared/TemplateVariableSubstitution.swift` | `{{date}}`/`{{time}}`/`{{datetime}}`/`{{filename}}`/`{{clipboard}}`/`{{author}}` placeholder resolution for snippet starter content. Pure Foundation, regex-based, never crashes on malformed or unknown placeholders — see its doc comment for the `{{filename}}` limitation. |
| `Shared/AuthorPreferenceStore.swift` | The `{{author}}` name set on the General preferences tab; plain-text file, same shared-storage pattern as the other Shared stores. |
| `CorrectClickExtension/FinderSync.swift` | FIFinderSync subclass; builds the "CorrectClick" menu dynamically from `MenuPreferencesStore` (enabled built-ins, in saved order) plus `UserTemplateStore` (enabled snippets, in sort order) as a third menu section, and dispatches to the right `FileCreator` action — built-ins via a static id→selector map, user templates via one generic handler keyed by the template's id in `NSMenuItem.representedObject` |
| `CorrectClickExtension/FileCreator.swift` | File creation logic, unique name generation (handles both `name.ext` and extensionless/dotfile names), rename-mode trigger, and `createFromUserTemplate` (resolves `{{…}}` placeholders before writing) |
| `CorrectClickExtension/CorrectClickExtension.entitlements` | Sandbox + filesystem temporary exception |
| `CorrectClick/AppDelegate.swift` | First-launch: opens System Preferences → Extensions |
| `CorrectClick/PreferencesView.swift` | Top-level Preferences window: a `TabView` of General / Menu Items / Snippets |
| `CorrectClick/GeneralPreferencesView.swift` | Author Name field (`{{author}}`) |
| `CorrectClick/MenuItemsPreferencesView.swift` | Per-item enable/disable toggle + drag-to-reorder within each section (New File, From Clipboard). Writes to `MenuPreferencesStore` immediately on every change — no separate "Apply" step, since the extension re-reads the file on every right-click. |
| `CorrectClick/SnippetsPreferencesView.swift` / `TemplateEditorView.swift` | Snippet management: add/edit/delete/reorder/enable-disable list, an edit sheet with a live preview (dummy placeholder values), and Import…/Export… via `NSOpenPanel`/`NSSavePanel` |
| `CorrectClick/PreferencesWindowController.swift` | Hosts `PreferencesView`, same floating-then-normal window-level pattern as `OnboardingWindowController` |
| `CorrectClick/UpdaterManager.swift` | Owns the single `SPUStandardUpdaterController` instance (Sparkle), created at launch in `AppDelegate` |
| `CorrectClick/UpdatesPreferencesView.swift` | Automatic-checks toggle + manual "Check for Updates Now" button |
| `CorrectClick/StatusBarController.swift` | Menu-bar icon: "Enable Extension…", "Preferences…", "Check for Updates…", "Uninstall CorrectClick…", Quit |
| `scripts/generate_appcast.sh` | Signs and merges a new appcast entry for a just-built release DMG; see `RELEASING.md` |
| `CorrectClickTests/` | Unit tests for `FileCreator.uniqueURL` naming, `MenuPreferencesStore` load/merge/save, `TemplateVariableSubstitution`, and `UserTemplateStore`/`UserTemplateOperations` CRUD + import/export round-tripping. Standalone `bundle.unit-test` target (no host app) — pulls `Shared/` and `CorrectClickExtension/FileCreator.swift` in directly as sources rather than `@testable import`, since the extension is an app-extension bundle, not a regular framework. Run via CI on every push to `main`, before the version bump/tag/release steps (see `.github/workflows/release.yml`). |
| `scripts/uninstall.sh` | Uninstaller — quits the app, disables/unregisters the extension, removes `/Applications/CorrectClick.app` and saved state. Bundled into the app at build time as `Contents/Resources/Uninstall CorrectClick.command` (see `project.yml`'s "Bundle Uninstaller" script) and also dropped loose into the DMG by `build_dmg.sh`, so it's reachable both from the running app and from the installer disk image. |

## Entitlements notes
- Parent app: sandboxed. Also holds `com.apple.security.temporary-exception.apple-events` scoped to `com.apple.Terminal` (+ `NSAppleEventsUsageDescription` in Info.plist) so the "Uninstall CorrectClick…" menu item can script Terminal to run the bundled uninstaller — `NSWorkspace.open()` alone can't hand a document to another app under App Sandbox ("not allowed to open documents in Terminal"), so it uses `NSAppleScript` + `do script` instead. First use prompts the user for Terminal-automation permission.
- Extension: sandboxed (`app-sandbox = true`) + `temporary-exception.files.absolute-path.read-write` with path `/` — this grants full filesystem read-write within the sandbox and is valid for notarised direct-download distribution. **App Store distribution would require a different architecture** (XPC relay: extension calls parent app via XPC, parent app holds user-selected file access and does the actual write).
- Parent app also holds a second `temporary-exception.files.absolute-path.read-write`, scoped narrowly to `~/Library/Application Support/CorrectClick/` — this is the shared menu-preferences file (`Shared/MenuItemPreferences.swift`). **Why this is needed:** the app and the extension are two separate processes with two separate sandbox containers, so neither one's own container is a place they can both reach. The file has to live at a real, absolute path outside both, so both sides need an entitlement that reaches it — the extension already has one (its blanket `/` exception covers this path too, no change needed there); the app didn't, so it got this narrower one instead of widening its own sandbox further.
- Both the app and extension resolve that shared path via `RealHomeDirectory.path` (`Shared/MenuItemPreferences.swift`), not `NSHomeDirectory()` / `FileManager.homeDirectoryForCurrentUser`. Under App Sandbox those APIs return the *container* path (e.g. `~/Library/Containers/com.correctclick.CorrectClick/Data`) even when a temporary-exception entitlement grants access elsewhere — they don't reflect what the entitlement actually unlocked. `RealHomeDirectory` reads the true home directory from `getpwuid(getuid())` instead, so both processes compute the identical real path and land on the same file. If a preferences-loading bug ever looks like "the extension and app see different files," check this first. `UserTemplateStore` (`templates.json`) and `AuthorPreferenceStore` (`author.txt`) reuse this same mechanism and folder.
- The app also holds `com.apple.security.files.user-selected.read-write`, a normal (non-temporary-exception) entitlement, for the Snippets tab's Import…/Export… buttons — they use `NSOpenPanel`/`NSSavePanel` to let the user pick an arbitrary file, which needs this even though it's unrelated to the shared-preferences path above.

## Uninstaller notes
- `scripts/uninstall.sh` can't fully clear `~/Library/Containers/com.correctclick.CorrectClick` when run from a plain shell/Terminal — macOS's TCC blocks that even for the owning user unless the calling app has Full Disk Access. The script warns and continues rather than aborting; the app itself is still fully removed. This is a real platform constraint, not a bug to "fix" — documented so it isn't rediscovered.
- Any shell command embedded in an AppleScript `do script` sent to Terminal needs its own shell-level quoting (separate from the AppleScript string escaping) — the bundled `.command` path contains a space and silently broke without it.

## macOS 26 pluginkit quirk
On macOS 26, each new debug build is registered as a new plugin instance in a pending-approval state, even if the extension is already enabled in System Settings. A post-build script in `project.yml` runs `pluginkit -e use -i <bundle-id>` automatically after each build to approve it. Without this, the extension runs but Finder ignores it (the menu does not appear).
